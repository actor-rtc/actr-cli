edition = 1
exports = []

[package]
name = "{{PROJECT_NAME}}-client-app"
description = "Echo client app"

[package.actr_type]
manufacturer = "acme"
name = "{{PROJECT_NAME_PASCAL}}Client"
authors = []
license = "Apache-2.0"
tags = ["dev", "client"]

[dependencies]

[system.signaling]
# When running on a physical device, do not use localhost/127.0.0.1.
url = "{{SIGNALING_URL}}"

[system.deployment]
realm = 0

[system.discovery]
visible = true

[scripts]
dev = "xcodegen generate"
test = "xcodebuild test"

[acl]

[[acl.rules]]
permission = "allow"
types = ["acme:EchoService", "acme:FileTransferService"]
